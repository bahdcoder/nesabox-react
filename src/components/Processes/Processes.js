import React from 'react'
import Daemon from 'pages/Daemon'

const Processes = props => {
    return (
        <React.Fragment>
            <Daemon {...props} />
        </React.Fragment>
    )
}

export default Processes
